name: flutter_counter
description: Example BLoC Counter App
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=2.18.6 <3.0.0"

dependencies:
  authentication:
    path: packages/authentication
  bloc_concurrency: ^0.2.0
  equatable: ^2.0.5
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.1
  formz: ^0.4.1
  http: ^0.13.5
  stream_transform: ^2.1.0
  user:
    path: packages/user

dev_dependencies:
  bloc_test: ^9.1.0
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  mocktail: ^0.3.0
  very_good_analysis: ^3.1.0

flutter:
  uses-material-design: true
